-- Criar o banco de dados (execute este comando separadamente se necessário)
-- CREATE DATABASE AgendaAppDb;

-- Usar o banco de dados
USE AgendaAppDb;

-- As tabelas serão criadas automaticamente pelo Entity Framework
-- quando você executar o comando: dotnet ef database update

-- Script para criar uma categoria padrão para usuários existentes (opcional)
-- Este script pode ser executado após a criação das tabelas pelo EF

-- Exemplo de inserção de dados de teste (opcional)
-- INSERT INTO Categories (Name, Color, UserId, CreatedAt)
-- VALUES ('Trabalho', '#EF4444', 'user-id-here', GETUTCDATE());

-- INSERT INTO Categories (Name, Color, UserId, CreatedAt)
-- VALUES ('Pessoal', '#10B981', 'user-id-here', GETUTCDATE());
